<!DOCTYPE html>
<html>
<head>
  <title>Socialtv</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <%= yield :after_body %>
  <div class="navbar navbar-static-top navbar-inverse">
    <div class="navbar-inner">
      <a class="brand" href="#">SocialTv</a>
      <ul class="nav pull-right">
        <% if current_user %>
        <li class="nav-username"><a href="#"><%= @current_user.name %></a></li>
        <li class="nav-settings"><a href="#" "S">S</a></li>
        <li class="nav-signout"><a href="/signout">Sign Out</a></li>
        <% else %>
        <li class="sign_in"><%= link_to "Sign in with fb", "/auth/facebook" %></li>
        <% end %>
      </ul>
    </div>
  </div>
  <div class="row-fluid">
    <div class="span12">
      <%= yield %>
    </div>
  </div>
</body>
</html>
